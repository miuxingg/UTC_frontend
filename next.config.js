/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
};

module.exports = {
  // reactStrictMode: true,
  // swcMinify: true,
  experimental: {
    scrollRestoration: true,
  },
};
module.exports = nextConfig;
