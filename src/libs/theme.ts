import { createMuiTheme } from '@mui/material';

export const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#b5ddd1',
    },
    secondary: {
      main: '#b2d9ea',
    },
    error: {
      main: '#eee',
    },
    background: {
      default: '#fff',
    },
  },
});
